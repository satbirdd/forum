<h1>Listing sections</h1>

<div class='sections'>
  <% @sections.each do |section| %>
        <div class='section'><%= link_to section.name, section %> </div>
  <% end %>
</div>

<%= link_to 'New Section', new_section_path %>

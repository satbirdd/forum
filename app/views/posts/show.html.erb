<p id="notice"><%= notice %></p>
<h1><%= @post.title %></h1>
<p><%= @post.body %></p>

<%= render 'comment_form' %>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', section_path(@post.section) %>
